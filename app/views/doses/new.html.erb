<div class="background-img page_wrapper">
  <div class="form">
    <h3>add doses now</h3>
    <div>
      <%= simple_form_for([@cocktail, @dose]) do |f| %>
      <%= f.input :description %>
      <%= f.association :ingredient %>
      <%= f.submit "add", class: "btn btn-dark" %>
      <% end %>
    </div>
  </div>
</div>
