<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
    <%= link_to "back to all cocktails", cocktails_path, class: 'btn btn-outline-dark btn-lg' %>
    <%= link_to "add details", new_cocktail_dose_path(@cocktail, @dose), class: 'btn btn-outline-dark btn-lg' %>
</div>
<div class="wrapper">
  <div class="show" style="background-image: url('<%= cl_image_path @cocktail.photo.key %>')">
    <h3 style="color :white; font-weight: bold"><%= @cocktail.name %></h3>
  </div>
<ul>
  <% @cocktail.doses.each do |d| %>
  <li>
    Ingredients: <%= d.ingredient.name %>
    <%= link_to dose_path(d), method: :delete do %>
      <%= '🗑' %>
    <% end %>
  </li>
  <% end %>
</ul>
</div>

