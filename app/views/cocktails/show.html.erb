<div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
    <%= link_to "back to all cocktails", cocktails_path, class: 'btn btn-dark' %>
    <%= link_to "add details", new_cocktail_dose_path(@cocktail, @dose), class: 'btn btn-dark' %>
</div>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-12 col-lg-6">
      <h3><%= @cocktail.name %></h3>
      <ul>
        <% @cocktail.doses.each do |d| %>
        <li>
          Ingredients: <%= d.ingredient.name %>
          <%= link_to dose_path(d), method: :delete do %>
            <%= '🗑' %>
          <% end %>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
