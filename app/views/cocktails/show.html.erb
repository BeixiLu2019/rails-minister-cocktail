<h3><%= @cocktail.name %></h3>
<ul>
  <% @cocktail.doses.each do |d| %>
  <li>
  Ingredients: <%= d.ingredient.name %>
  <%= link_to dose_path(d), method: :delete do %>
  <%= '🗑' %>
  <% end %>
  </li>
  <% end %>
</ul>
<%= link_to "add details", new_cocktail_dose_path(@cocktail, @dose) %>
<%= link_to "back to all cocktails", cocktails_path %>
